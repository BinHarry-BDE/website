{
  "name": "binharry-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && npm run clean:cache",
    "clean:cache": "node -e \"const fs = require('fs'); const path = require('path'); const cacheDir = path.join('.next', 'cache'); if (fs.existsSync(cacheDir)) { fs.rmSync(cacheDir, { recursive: true, force: true }); console.log('Cache cleared'); }\"",
    "start": "next start",
    "lint": "next lint",
    "deploy": "echo 'Deployed to Cloudflare Pages'"
  },
  "dependencies": {
    "next": "^15.1.12",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
